services:
  - type: web
    name: AI-Assessment-Recommendation-Agent
    env: python
    plan: free
    pythonVersion: "3.10.11"

    buildCommand: |
      pip install -r requirements.txt
      python app/embeddings.py

    startCommand: |
      uvicorn app.main:app --host 0.0.0.0 --port $PORT